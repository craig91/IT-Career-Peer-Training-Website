name: CI Pipeline


on:
  push:
    branches:
    - main


jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    # - name: Set up Node.js
    #   uses: actions/setup-node@v3
    #   with:
    #     node-version: '14'

    # - name: Install Dependencies
    #   run: npm install

    # - name: Run build
    #   run: npm run build

    # - name: Set up SSH
    #   run: |
    #     mkdir -p ~/.ssh
    #     if [ ! -f ~/.ssh/id_rsa ]; then echo "${{ secrets.SSH_PRIVATE_KEY }}" | tr -d '\r' > ~/.ssh/id_rsa; fi
    #     chmod 600 ~/.ssh/id_rsa
    #     ssh-keyscan -H 192.168.1.213 >> ~/.ssh/known_hosts
    #   shell: bash

    - name: Deploy to server
      run: |
        scp -r ./ cdejean@192.168.1.213:/var/www/html
      env:
        SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

